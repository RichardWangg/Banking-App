import React, { useRef } from 'react';
import { View } from 'react-native';
import transactionStyles from '../styles/transactionInputStyles';

export default function Transaction({ title, price }) {


    return (
        <View>

        </View>
    )
}